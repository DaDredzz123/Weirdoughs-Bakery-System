# Weirdoughs-Bakery-System
This System is an automatic bakery system that has both an admin and a customer interface. The functions in admin adds and deletes products and the customer shows a login interface, menu and receipt.
When running the python files, run Admin.py to access admin. Run Main.py to access introduction and menu.
